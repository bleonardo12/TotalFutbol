/** De mejor a peor (concepto.md §6). */
export type Division = "ELITE" | "ORO" | "PLATA" | "BRONCE";

/** Pools de ranking completamente separados, fijos por equipo (Hito 6c). */
export type CategoriaFutbol = "MASCULINO" | "FEMENINO" | "MIXTO";
